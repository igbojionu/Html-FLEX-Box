
```

Explanation:

1. **Resetting Default Styles:** We start by resetting the default margin and padding of the body and some heading elements to ensure consistent styling.

2. **Flex Container Setup:** We create a flex container using the `.flex-container` class. This container will hold our flex items.

3. **Flex Item Styling:** We define the styling for the flex items using the `.flex-item` class. In this example, each item represents a menu item.

4. **Flex Property:** The `flex: 1;` property is used to make the flex items flexible and take up equal space within the flex container.

5. **Media Query for Responsiveness:** We use a media query to change the flex container's direction to column on smaller screens, providing a responsive layout.

Feel free to experiment with the values and add more items to the menu to see how Flexbox adjusts the layout.